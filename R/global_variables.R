globalVariables(
  names = c(
    #nothing in this case but given the issue i ran into with tidyDenovix,
    #the omnipresence of the tidyverse in R programming ,and learning
    #from Steven Anderson in his tidyAML package, i should have this
    #file in all my packages as a placeholder.
  )
)
